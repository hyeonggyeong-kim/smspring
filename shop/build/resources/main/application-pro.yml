
# springboot 내장 서버 환경 설정
# bootwar 실행시에만 적용
# war tomcat 실행 할때는 적용 안됨

server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12
    key-store-password: ENC(wcpLb3m0/NbJ2urXvQXQiQ==)
    key-store-type: PKCS12
  servlet:
    encoding:
      charset: UTF-8
      force: true
spring:
  mvc:
    view:
      prefix: /views/
      suffix: .jsp
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shopdb?serverTimezone=Asia/Seoul
    username: smuser
    password: ENC(wcpLb3m0/NbJ2urXvQXQiQ==)

mybatis:
  type-aliases-package: edu.sm.app.dto
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    root: error

# Application Config
app:
  key:
    skey: mykey